- name: Load and execute every role
  hosts: webservers
  tasks:
    - name: Load and execute NGINX role
      ansible.builtin.include_role:
        name: roles/nginx
    - name: On charge et lance le rôle SSH
      ansible.builtin.include_role:
        name: roles/ssh
